<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login | Suly Cinema</title>
    <link rel="stylesheet" href="/logincss.css" />
</head>

<body>
    <div class="wrapper">
        <h1>🎬 Suly Cinema</h1>
        <h2>Changing Name</h2>

        <ul class="messages">
            <% if (typeof errors !='undefined' ) { %>
                <% errors.forEach(error=> { %>
                    <li class="error">
                        <%= error.message %>
                    </li>
                    <% }) %>
                        <% } %>

                            <% if (messages.success_msg) { %>
                                <li class="success">
                                    <%= messages.success_msg %>
                                </li>
                                <% } %>

                                    <% if (messages.error) { %>
                                        <li class="error">
                                            <%= messages.error %>
                                        </li>
                                        <% } %>
        </ul>

        <form action="/editProfile" method="post">
            <div class="input-box">
                <input name="name" type="text" placeholder="Enter your new name" value="<%= current_name  %>"
                    required />
                <div class="input-box button">
                    <input type="submit" value="Submit" />
                </div>


        </form>
    </div>
</body>

</html>